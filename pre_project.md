你将作为本项目的主要开发助手。

一、项目目标

开发一个移动端优先的 2D Pixel Art H5 小游戏，整体画风和体验参考 Stardew Valley 一类的 2D 像素游戏。

核心要求：

* 使用浏览器运行，不开发原生 App
* 用户通过 URL、二维码或 NFC 进入游戏
* 不依赖游戏后端和数据库
* 游戏本体采用纯静态资源部署
* 首次访问尽可能快速加载
* 首次加载完成后支持离线运行
* 支持手机和 PC 浏览器
* 支持 GitHub 进行版本管理和线上部署
* 后续可以扩展地图、NPC、对话、动画、音效等内容

二、开发环境

开发机器：

* macOS
* Apple Silicon
* Cursor 作为主要 IDE
* GitHub 作为代码仓库

计划技术栈：

* TypeScript
* Phaser 3
* Vite
* Tiled：地图制作
* Aseprite：Pixel Art 素材制作
* PWA / Service Worker：离线运行和资源缓存

线上部署以静态网站托管为原则，例如 GitHub Pages、Cloudflare Pages 或其他类似服务。

三、开发前请先做环境检查

在开始编写游戏代码之前：

1. 检查 Node.js / npm 是否已经安装以及版本。
2. 检查 Git 是否正常。
3. 检查 GitHub CLI（gh）是否安装；如果没有，只说明缺少，不要擅自安装。
4. 检查当前目录是否已经是 Git 仓库。
5. 检查 Cursor 当前打开的项目目录。
6. 检查是否已经安装或准备使用 Tiled 和 Aseprite。
7. 检查是否存在现有项目文件，避免覆盖用户已有内容。

四、在正式开发前输出一份简短方案

请先不要大量创建代码。

先根据当前环境给出：

1. 环境检查结果
2. 推荐项目目录结构
3. Phaser + Vite + TypeScript 初始化方案
4. PWA / Service Worker 离线缓存方案
5. 静态资源组织和加载策略
6. GitHub 仓库和版本管理方案
7. 本地开发测试方式
8. 手机局域网测试方式
9. Production build 和线上测试方式
10. 后续二维码 / NFC 使用方案

重点考虑：

* 首次加载速度
* 游戏资源体积
* 图片压缩
* 音频体积
* JS bundle 大小
* 缓存策略
* 离线运行
* 移动端性能
* 不依赖后端
* 以后扩展游戏内容时不会导致项目结构失控

五、重要设计原则

本项目是“小型精致 H5 游戏”，不是大型游戏项目。

因此：

* 保持项目结构简单
* 不要引入 ECS
* 不要过度设计架构
* 不要为了简单功能创建大量抽象层
* 优先使用 Phaser 原生能力
* 尽量减少第三方依赖
* 游戏逻辑和资源管理保持清晰
* 移动端优先
* 默认使用较小的逻辑分辨率和 Pixel Art 渲染
* 所有资源应考虑移动端加载速度
* 不需要后端、数据库或账号系统，除非后续明确提出

六、开发流程

后续开发按照以下顺序推进：

环境检查
→ 项目初始化
→ Phaser 最小 Demo
→ Player 移动
→ 地图和碰撞
→ 手机触摸控制
→ Tiled 地图
→ Aseprite 素材
→ 动画
→ NPC / 对话
→ 音效
→ PWA / 离线缓存
→ Production Build
→ GitHub
→ 线上测试
→ 二维码 / NFC

当前阶段只负责：

环境检查 + 项目初始化方案。

不要直接开始实现完整游戏。

所有代码新增或修改，都必须以文件 Diff 的形式展示，等待我确认后再进行下一步。

读取、查找、编译、测试等不会修改项目内容的命令可以直接执行。